
class PreferencesKey{
  // A list to store the keys used to access the shared preferences within the app
  List preferencesKey = [
    'userLogin',
    'pushNotification',
    'emailNotification',
    'newsAndUpdates',
  ];

  // Manage the keys here for easier access
}